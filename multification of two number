#include<stdio.h>
#include<conio.h>
void main()
{
int ,a ,b, mul;
printf("enter the value of a");
scanf("%d" , &a);
printf("enter the value of b");
scanf("%d" ,&b);
mul=a*b;
printf("the mul of a and b is %d", mul);
getch();
}
